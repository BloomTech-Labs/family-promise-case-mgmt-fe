import React from 'react';

import search from '../../../assets/searchIcon.png';

const SearchIcon = () => {
  return (
    <div>
      <img src={search} alt="magnifying glass on the search bar" />
    </div>
  );
};

export default SearchIcon;
